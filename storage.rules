rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // COC dokümanları — auth zorunlu (login olmuş kullanıcı)
    match /appData/{path=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
